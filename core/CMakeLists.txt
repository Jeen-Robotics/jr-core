add_subdirectory(math)
add_subdirectory(imgproc)
add_subdirectory(middleware)
add_subdirectory(bag)
add_subdirectory(nodes)
add_subdirectory(vio)

if(BUILD_RUST_MIDDLEWARE)
    add_subdirectory(middleware_rs)
endif()
